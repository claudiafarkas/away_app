import 'package:flutter/material.dart';

class MyImportsTab extends StatelessWidget {
  const MyImportsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('My Videos'));
  }
}
